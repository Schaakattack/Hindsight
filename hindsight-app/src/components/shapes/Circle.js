
const Circle = (id, x, y, r)=>{
    return {
        id: id,
        x: x,
        y: y,
        r: r,
        fill: 'none',
        stroke: '#FFB323',
        type: 'circle'
    }
}

export default Circle;